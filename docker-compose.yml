version: "3.8"
services:
  wordcount:
    image: wordcount-app:latest
    deploy:
      replicas: 2
      restart_policy:
        condition: none
    volumes:
      - /root/docker_project/data:/home/data
